#ifndef LETTERS_HPP
#define LETTERS_HPP
#include <iostream>
#include <fstream>
using std::ifstream;
using std::ofstream;

void count_letters(ifstream &, int*);
void output_letters(ofstream &, int*);

#endif